[workspace]
resolver = "2"
members = ["crates/waggledance-core", "crates/waggledance"]
# Desktop shell (Tauri) is excluded from the default workspace build because it
# requires system libraries (webkit2gtk/gtk3 on Linux) not present everywhere.
# Build it explicitly from its own directory — see crates/waggledance-desktop/README.md.
exclude = ["crates/waggledance-desktop"]

[workspace.package]
version = "0.5.2"
edition = "2021"
license = "MIT"
authors = ["Van Tran"]
repository = "https://github.com/thanhsmind/waggledance"

[workspace.dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = { version = "1", features = ["preserve_order"] }
toml = "0.8"
toml_edit = "0.22"
thiserror = "1"
anyhow = "1"
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
dirs = "5"
time = { version = "0.3", features = ["formatting", "parsing", "serde"] }

# rendering
comrak = "0.29"
syntect = "5"
ammonia = "4"

# storage / indexing
rusqlite = { version = "0.32", features = ["bundled"] }
ignore = "0.4"
notify = "6"
notify-debouncer-full = "0.3"
nucleo-matcher = "0.3"

# server / cli
axum = { version = "0.7", features = ["ws"] }
tokio = { version = "1", features = ["full"] }
tower-http = { version = "0.6", features = ["cors", "trace"] }
clap = { version = "4", features = ["derive"] }
minijinja = { version = "2", features = ["loader"] }
async-trait = "0.1"
interprocess = { version = "2.4.2", features = ["tokio"] }
rand = "0.8"

# platform
libc = "0.2"

[profile.release]
opt-level = "s"
lto = true
codegen-units = 1
panic = "abort"
strip = true

# Local dev loop. `release` stays tuned for the shipped binary (fat LTO,
# codegen-units = 1) — both of which force the final codegen onto a single
# thread and pin one core for minutes. `fast` trades binary size and a little
# runtime speed for a build that actually uses every core.
# Build with: cargo build --profile fast -p waggledance  (binary at target/fast/)
[profile.fast]
inherits = "release"
opt-level = 1
lto = false
codegen-units = 16
incremental = true
strip = "debuginfo"
