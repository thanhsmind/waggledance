promote proposal for work item "bee-board-pm" (docs/history/bee-board-pm/CONTEXT.md + docs/history/bee-board-pm/plan.md) — 18 capped cell(s): bbp-1, bbp-2, bbp-3, bbp-4, bbp-5, bbp-6, bbp-7, bbp-8, bbp-9, bbp-10, bbp-11, bbp-12, bbp-13, bbp-14, bbp-15, bbp-16, bbp-17, bbp-18
anchor: history — docs/history/bee-board-pm/CONTEXT.md, docs/history/bee-board-pm/plan.md
PROPOSAL ONLY — nothing was written. Applying any section below is a human or agent decision.

(a) DELIVERY DRAFT — save as docs/knowledge/work/bee-board-pm/delivery.md

---
type: bee.delivery
title: bee-board-pm — delivery
description: "Delivery record proposed by bee knowledge promote for work item bee-board-pm: 18 capped cell(s), 0 recorded deviation(s)."
timestamp: 2026-08-06
bee:
  id: bee-board-pm-delivery
  lifecycle: active
  areas: [bee-cockpit]
  required_context: [docs/history/bee-board-pm/CONTEXT.md, docs/history/bee-board-pm/plan.md]
  sources: [docs/history/bee-board-pm/CONTEXT.md, docs/history/bee-board-pm/plan.md, .bee/cells/bbp-1.json, .bee/cells/bbp-2.json, .bee/cells/bbp-3.json, .bee/cells/bbp-4.json, .bee/cells/bbp-5.json, .bee/cells/bbp-6.json, .bee/cells/bbp-7.json, .bee/cells/bbp-8.json, .bee/cells/bbp-9.json, .bee/cells/bbp-10.json, .bee/cells/bbp-11.json, .bee/cells/bbp-12.json, .bee/cells/bbp-13.json, .bee/cells/bbp-14.json, .bee/cells/bbp-15.json, .bee/cells/bbp-16.json, .bee/cells/bbp-17.json, .bee/cells/bbp-18.json]
---

# bee-board-pm — Delivery

## What shipped

- **bbp-1** — Added scrub_paths beside relativize and wired it into every free-text field read_snapshot already produces (cell title, lane next_action, backlog pbi title/finding title+detail, decision text) (1 file(s) changed)
- **bbp-2** — snapshot_tree now records directories via a TreeEntry enum, sorted deterministically by path; new tests prove an empty created directory flips the comparison and an empty tree self-compares equal (1 file(s) changed)
- **bbp-3** — Extended BeeState with approved_gates, gate_revoked_at, route and next_action, scrubbed and version-drift tolerant (1 file(s) changed)
- **bbp-4** — Added compute_attention_items (D6): blocked cells and read_errors each fire independently, sorted heaviest-first with a stable tie-break, wired into BeeSnapshot.attention. (1 file(s) changed)
- **bbp-5** — Rebuilt the board's top to answer in D5's order: header, lifecycle stepper, headline numbers, working-on-now card, and the D6 attention panel, all above the untouched existing sections (2 file(s) changed)
- **bbp-6** — scrub_paths now finds an absolute path inside a delimiter-wrapped or punctuation-trailed token (parens, quotes, backticks, brackets, angle brackets, comma/period/semicolon) and Windows-shaped paths, reduces it, and re-emits the trimmed wrap (1 file(s) changed)
- **bbp-7** — Currently-approved gate now renders as approved regardless of an earlier gate_revoked_at entry; revocation only labels an undone gate as revoked vs never-reached; fixed the wrong-rule test and added regression/happy/edge coverage at view and route level (3 file(s) changed)
- **bbp-8** — Added a HANDOFF.json reader carrying written_at/next_action/kind onto BeeSnapshot (next_action scrubbed via scrub_paths) and a new D6 attention rule reporting a pause/kindless handoff with its own text and date, silent on planned-next; new reader, attention-rule and route-level read-only tests added, verified against this repo's live store. (2 file(s) changed)
- **bbp-9** — Added the .bee/config.json gate_bypass reader, normalized defensively, and a new attention rule reporting it as the recorded (not effective) level (2 file(s) changed)
- **bbp-10** — Placed every feature on its phase via the union of lanes and the active feature, with per-feature gates/created_at and D8-safe cell counts (2 file(s) changed)
- **bbp-11** — Replaced the four cell-state columns with a by-phase board (bee_phase_board_section) and re-targeted the finished-work list (bee_finished_section) onto snapshot.shipped, deduped against the phase board so a feature never renders twice; retired bee_lanes_panel. Fixed a live 404 found on the rendered beehive store: bee_feature_detail only recognized a feature through live D7 buckets/shipped, so every phase card for a lane-only, cells-archived feature 404'd. All 14 must-survive rules re-expressed against the new markup; the two 3-way tests split per area. Workspace rendering (half of the old lanes panel) is retired with no replacement in this cell, per the plan's S4 slice. (2 file(s) changed)
- **bbp-12** — Added validate_feature_name as the sole path-join gate for feature strings, plus presence-only promote-proposals.md reads over state/lane/cell feature names, with tests asserting no path is built for rejected names (1 file(s) changed)
- **bbp-13** — Added review-candidate/session join, capture-queue net-waiting count, and per-feature scribing-debt derivation to the bee reader, plus three new D6/D7-worded attention rules and their read-only board fixtures (2 file(s) changed)
- **bbp-14** — Rendered the backlog by status and the review queue by state (unreviewed/in-review/settled, open P1s called out) as the board's last supporting panel; a store with no candidates and no sessions reads as unknown, not zeros; fixed a real-store defect where the new PBI-title list dumped all 123 of beehive's items uncapped (2 file(s) changed)
- **bbp-15** — Added .bee/reservations.json reader and pure tier-mix derivation to BeeSnapshot for the process-health panel (2 file(s) changed)
- **bbp-16** — Fold delivery speed, sessions, worktrees, workspaces and process health into the redesigned board, retiring the running-now and worktree sections without losing any signal (2 file(s) changed)
- **bbp-17** — Board now collapses to one column under 700px, gives its wide phase-board columns their own overflow, adds a visible focus ring to the finished-work summary, and proves dark-scheme rules exist with no OS media-query hook that could override an explicit theme choice in either direction — verified against the real beehive store at 375px in both schemes. (2 file(s) changed)
- **bbp-18** — Rewrote docs/specs/bee-cockpit.md to describe the board as built (reading order, lifecycle stepper, needs-attention rules, work-by-phase union rule, both guarantees, gate-bypass caveat), verified section by section against the live board rendered for this repo's own store and beehive's; reading-map.md's entry points were checked and are already accurate, no edit needed. (1 file(s) changed)

## Verify

Each cell below was capped only against a recorded passing verify result — bee refuses a cap without one.

- **bbp-1** — `cargo test --workspace. New unit tests in bee.rs's own test module drive the scrubber directly: (happy) a path embedded mid-sentence under the project root is reduced while the surrounding words survive; (happy) several paths in one string are all reduced; (edge) a string containing no path is returned byte-identical; (edge) an empty string; (edge) a string that is wholly an absolute path still behaves exactly as relativize does; (edge) a path outside the project root is redacted, not relativized; (regression) every existing relativize and rel_str test stays green.`
- **bbp-2** — `cargo test --workspace. New test: an empty directory created under the fixture .bee/ between snapshots makes the byte-identical assertion fail — proven by asserting the two snapshots differ, not by deleting the guard. Existing read-only tests reading_never_writes_the_fixtures_bee_tree, velocity_read_never_writes_the_fixtures_bee_tree, panels_read_never_writes_the_fixtures_bee_tree, detail_routes_never_write_the_fixtures_bee_tree, running_section_read_never_writes_the_fixtures_bee_tree and worktree_read_never_writes_the_project_or_sibling_bee_tree all stay green.`
- **bbp-3** — `cargo test --workspace. New reader-level tests in bee.rs's test module: (happy) a full state.json yields all four gates, the route with every field, and the next action; (edge) a state.json missing approved_gates, route and next_action degrades silently — no read_error, no fabricated values; (edge) a route carrying an unknown extra key is read without complaint; (edge) gate_revoked_at naming execution while approved_gates says execution is true is carried through so the view can tell them apart; (security) an absolute path embedded in route.rationale and in next_action does not survive into the snapshot; (regression) every existing read_state test stays green.`
- **bbp-4** — `cargo test --workspace. New unit tests driving the computation directly: (happy) a snapshot with blocked cells yields exactly that item, with a suggested action; (happy) a snapshot with read_errors yields exactly that item; (edge) a snapshot tripping both yields both, heaviest first, in a stable order; (edge) a clean snapshot yields an empty list; (edge) two items of the same severity come back in a deterministic order across repeated calls; (regression) computing the list perturbs no other snapshot field.`
- **bbp-5** — `cargo test --workspace. New route-level tests through tower ServiceExt oneshot: (happy) the board names the active feature, its stepper state and its next action; (happy) a feature with three of four gates approved renders three done steps and review as user-invoked, not pending; (edge) a revoked execution gate does not render as approved; (edge) a feature whose cells are all dropped renders an honest progress state with no division artifact; (edge) an empty attention list renders one line, not an empty panel; (untrusted) a cell title and a next_action containing < > & " render as text and do not break the markup; (security) the body contains no absolute path and no occurrence of the fixture root; (read-only) the fixture .bee/ tree is byte-identical after the request. THREE EXISTING WHOLE-BODY TESTS MUST STAY GREEN and are part of this cell's proof: no_shipped_features_renders_honest_empty_state_not_zeros (server.rs:2731) forbids the substrings 0.0 and 0/0 anywhere in the body and its fixture has no state.json at all; board_renders_finished_work_in_exactly_one_place (server.rs:3434) asserts a feature name appears exactly twice, so a new section naming it a third time breaks it; detail_pages_leak_no_absolute_path_or_fixture_root (server.rs:3634).`
- **bbp-6** — `cargo test --workspace. New unit tests beside the existing scrub_paths tests, each asserting the wrapped absolute path does NOT survive while the surrounding characters do: a path in parentheses; a path in double quotes; a path in single backticks; a path in square brackets; a path in angle brackets; a path followed by a comma, by a period and by a semicolon; a path both wrapped and outside the root, which must reduce to the shared redaction text. Negative cases proving the scrubber did not become a blunt redactor: a relative path in backticks is unchanged; a bare filename in parentheses is unchanged; a string of ordinary prose is byte-identical. Every existing scrub_paths, relativize and rel_str test stays green.`
- **bbp-7** — `cargo test --workspace. New tests, at both the level the rule lives at and through the route: (regression, the live defect) approved_gates.execution true with gate_revoked_at.execution set renders Execute as approved, NOT as revoked — assert the rendered body does not carry the revoked wording for that step; (happy) approved_gates.execution false with gate_revoked_at.execution set renders Execute as revoked, distinguishable from a step that was never approved; (happy) approved_gates.execution false with no gate_revoked_at renders Execute as not yet reached; (edge) gate_revoked_at present but naming a different gate does not affect Execute; (edge) gate_revoked_at absent entirely behaves as before. Every existing bbp-5 stepper test stays green, updated where it encoded the wrong rule — and any test that did encode the wrong rule must be named in the outcome, not silently rewritten.`
- **bbp-8** — `cargo test --workspace. Reader-level tests: (happy) a full HANDOFF.json is carried onto the snapshot with all three fields; (edge) no HANDOFF.json at all — snapshot carries none, and NO read_error is pushed; (edge) a malformed HANDOFF.json pushes exactly one read_error and the rest of the snapshot still reads; (edge) a handoff with no `kind` key; (security) an absolute path embedded mid-sentence in next_action, and one wrapped in backticks, do not survive onto the snapshot. Attention-rule tests: (happy) a pause handoff yields exactly one item carrying the note's text and its written_at; (happy) a planned-next handoff yields no pause item; (edge) no handoff yields no item; (edge) a handoff alongside blocked cells yields both, ordered heaviest-first. Read-only: a NEW test whose fixture CONTAINS a populated .bee/HANDOFF.json asserts the tree is byte-identical after a board request — the six existing read-only tests use fixtures with no handoff file, so they would pass without ever running this reader.`
- **bbp-9** — `cargo test --workspace. Reader-level tests: (happy) gate_bypass "total" is carried through as itself; (happy) gate_bypass false is carried as off; (edge) no gate_bypass key at all reads as off; (edge) no .bee/config.json at all reads as off and pushes NO read_error; (edge) a malformed config.json pushes exactly one read_error and the rest of the snapshot still reads; (edge) an unrecognised value is carried through, not coerced. Attention-rule tests: (happy) a recorded bypass that is not off yields exactly one item naming the recorded level; (edge) off yields no item; (edge) the item's wording does not claim the level is effective — assert the rendered wording marks it as recorded. Read-only: a NEW test whose fixture CONTAINS a populated .bee/config.json asserts the tree is byte-identical after a board request.`
- **bbp-10** — `cargo test --workspace. Reader-level tests: (happy) a lane record's own approved_gates and created_at are carried; (happy) several lane records plus a distinct active feature produce the union, with the active feature present exactly once; (edge) the active feature ALSO having a lane record produces it exactly once, not twice, and the lane record wins for its own phase and gates; (edge) no .bee/lanes/ directory at all yields exactly the active feature, placed on the phase state.json names; (edge) a lane record whose feature has no cells reports zero counts without dividing by zero; (edge) a feature whose cells are all dropped reports no completed work and no division artifact (D8); (edge) a malformed lane file pushes one read_error and the other lanes still read; (security) an absolute path in a lane's next_action does not survive onto the snapshot. Fixture: lane_json must now express approved_gates and created_at, and every existing test using it stays green.`
- **bbp-11** — `cargo test --workspace. New route-level tests through tower ServiceExt oneshot: (happy) several features render on their phases, each linking to its feature detail page; (happy) the active feature appears exactly once; (edge) a repo with no lane records renders its one feature honestly, not an empty board; (edge) a feature with no cells renders an honest empty state, no division artifact; (edge) a feature whose cells are all dropped shows no completed work; (regression) the fourteen rules listed in the action, each re-expressed against the new markup; (untrusted) a feature name and a next_action containing < > & " render as text; (security) no absolute path and no fixture root in the body; (read-only) the fixture .bee/ tree byte-identical after the request. The old four-bucket and lanes-panel tests are either re-expressed or named as retired in the outcome — never silently deleted.`
- **bbp-12** — `cargo test --workspace. Validator unit tests, each asserting NO path is built: a feature named `../../etc`; one named `../..`; one containing a forward slash; one containing a backslash; an absolute POSIX path; a Windows drive-prefixed path; a leading-dot name; an empty string; a name with a control character. Positive cases: an ordinary slug passes; a slug with hyphens and digits passes. Integration tests: (happy) a feature whose promote-proposals.md exists is reported as having proposals; (happy) one whose file does not exist is reported as not having them; (security) a cell and a state.json each carrying a traversal-shaped feature produce NO filesystem lookup outside the project — assert on the built path set, not merely on the rendered output, so the probe fails if the lookup happens and the result is merely hidden; (edge) a validation failure pushes no read_error. Existing tests all stay green.`
- **bbp-13** — `cargo test --workspace. Review-join tests: (happy) a candidate in no session is unreviewed; (happy) a candidate whose cell is in a pending session is in review; (happy) a candidate whose cell is in an approved session is settled; (happy) the same for a blocked session; (edge) a session naming a cell that no longer exists does not crash and does not lose the session's other cells; (edge) a candidate naming zero cells behaves as pinned, with the choice stated in a comment; (edge) a finding with no severity key, and one with severity `info`, are counted honestly and never as P1; (edge) a session with no decision at all. Capture-queue tests: stubs and flushes counted correctly; absent file is silent, no read_error; a malformed line costs one read_error and not the good rows. Scribing-debt tests: a feature with capped behavior_change cells and no matching last_scribing_run has debt; one whose last_scribing_run names it does not; a feature with no behavior_change cells never has debt. Attention-rule tests: each of the three rules alone; all three together ordered heaviest-first; none of them firing on a clean store. Read-only: one NEW test per new file, each fixture CONTAINING that file populated, asserting the tree is byte-identical after a board request.`
- **bbp-14** — `cargo test --workspace. New route-level tests: (happy) backlog counts by status render and match the store; (happy) the review queue renders unreviewed, in-review and settled counts with open P1s called out; (edge) a store with no review candidates and no sessions renders an honest unknown state, not zeros; (edge) a store with candidates but no sessions renders them all as unreviewed; (edge) a capped list states its true total; (untrusted) a PBI title containing < > & " renders as text; (security) no absolute path and no fixture root in the body; (read-only) the fixture .bee/ tree byte-identical after the request. Existing backlog and findings tests are re-expressed against the new markup, or named as retired in the outcome with the reason.`
- **bbp-15** — `cargo test --workspace. Reservations tests: (edge) no .bee/reservations.json at all reads as empty with NO read_error; (happy) an empty {"reservations": []} reads as empty; (happy) a populated array carries every entry; (edge) a malformed file pushes exactly one read_error and the rest of the snapshot still reads; (edge) an unexpected shape — an object where an array belongs — reads as absent, not as an error. Tier-mix tests: (happy) a mix across tiers reports each count and the expensive-tier share; (edge) cells with no tier are counted as untiered, never dropped and never guessed; (edge) zero tiered cells reports no share rather than a zero or a division by zero; (edge) an empty cell store reports nothing rather than zeros. Read-only: a NEW test whose fixture CONTAINS a populated reservations.json asserts the tree is byte-identical after a board request.`
- **bbp-16** — `cargo test --workspace. New route-level tests: (happy) delivery-speed numbers render for a store with shipped work; (edge) nothing shipped renders an honest empty state with no NaN/Infinity/0.0/0-0; (happy) sessions, worktrees and workspaces render in one panel with liveness in plain words; (edge) an unresolved worktree grant still reports unresolved and the page still renders; (regression) a live worker's cell links to its detail page; (regression) a worker naming a cell the store calls open states the disagreement; (regression) a worker naming an unknown cell is flagged, not dropped; (regression) a worker on a stale session is absent from running work; (regression) the old running-now section is gone and no work renders twice; (happy) process health renders lock contention, tier spread and the recorded bypass setting; (edge) a store with read_errors shows them; (untrusted) a worker nickname and a branch name containing < > & " render as text; (security) no absolute path and no fixture root in the body; (read-only) the fixture .bee/ tree byte-identical after the request. Any test deleted rather than re-expressed is named in the outcome with the reason.`
- **bbp-17** — `cargo test --workspace. Because most of this is CSS and markup rather than logic, assert on what the rendered body actually contains, in route-level tests: (responsive) the board declares a narrow-screen rule collapsing its multi-column grids, and every horizontally-wide container carries its own overflow rule — assert the rules are present in the rendered stylesheet, and assert no fixed pixel width large enough to force a 375px page to scroll; (theme) the page's dark-scheme rules are present AND an explicit theme attribute overrides the OS preference in both directions — assert both override directions, since covering only one is the common half-fix; (colour) every severity or state chip carries text alongside its colour — assert that no severity is expressed by a class alone with no accompanying word; (keyboard) the finished-work disclosure is a natively focusable element rather than a click-only div, and any custom interactive element carries a tabindex and a visible focus style. Existing board tests all stay green.`
- **bbp-18** — `cargo test --workspace stays green (this cell changes documentation only, so the suite is a regression check, not the proof). The proof is the spec itself: every section above present, no Rust type, module path or function name anywhere in it, and every claim it makes checked against the rendered board on a real store rather than against the plan. State in the outcome which claims you verified against the live page and how.`

## Deviations

None recorded in the capped cell traces.

## Provenance

Proposed by `bee knowledge promote --work bee-board-pm` from 18 capped cell trace(s) in `.bee/cells/` and the anchor `docs/history/bee-board-pm/CONTEXT.md`, `docs/history/bee-board-pm/plan.md`. Every line above is copied from a trace or from the work item; nothing here is curated truth until a human or agent accepts it.

(b) AREA UPDATES — candidate spec-sync bullets, each citing its cell

areas: from the scribing stamp for "bee-board-pm" — .bee/logs/scribing-runs.jsonl's most recent entry (2026-08-06T19:40:10.153Z), the work item declares no bee.areas.

area bee-cockpit:
  - [bbp-1] Added scrub_paths beside relativize and wired it into every free-text field read_snapshot already produces (cell title, lane next_action, backlog pbi title/finding title+detail, decision text) — feature-wide sync per the scribing stamp, 1 file(s) changed (trace .bee/cells/bbp-1.json)
  - [bbp-3] Extended BeeState with approved_gates, gate_revoked_at, route and next_action, scrubbed and version-drift tolerant — feature-wide sync per the scribing stamp, 1 file(s) changed (trace .bee/cells/bbp-3.json)
  - [bbp-4] Added compute_attention_items (D6): blocked cells and read_errors each fire independently, sorted heaviest-first with a stable tie-break, wired into BeeSnapshot.attention. — feature-wide sync per the scribing stamp, 1 file(s) changed (trace .bee/cells/bbp-4.json)
  - [bbp-5] Rebuilt the board's top to answer in D5's order: header, lifecycle stepper, headline numbers, working-on-now card, and the D6 attention panel, all above the untouched existing sections — feature-wide sync per the scribing stamp, 2 file(s) changed (trace .bee/cells/bbp-5.json)
  - [bbp-6] scrub_paths now finds an absolute path inside a delimiter-wrapped or punctuation-trailed token (parens, quotes, backticks, brackets, angle brackets, comma/period/semicolon) and Windows-shaped paths, reduces it, and re-emits the trimmed wrap — feature-wide sync per the scribing stamp, 1 file(s) changed (trace .bee/cells/bbp-6.json)
  - [bbp-7] Currently-approved gate now renders as approved regardless of an earlier gate_revoked_at entry; revocation only labels an undone gate as revoked vs never-reached; fixed the wrong-rule test and added regression/happy/edge coverage at view and route level — feature-wide sync per the scribing stamp, 3 file(s) changed (trace .bee/cells/bbp-7.json)
  - [bbp-8] Added a HANDOFF.json reader carrying written_at/next_action/kind onto BeeSnapshot (next_action scrubbed via scrub_paths) and a new D6 attention rule reporting a pause/kindless handoff with its own text and date, silent on planned-next; new reader, attention-rule and route-level read-only tests added, verified against this repo's live store. — feature-wide sync per the scribing stamp, 2 file(s) changed (trace .bee/cells/bbp-8.json)
  - [bbp-9] Added the .bee/config.json gate_bypass reader, normalized defensively, and a new attention rule reporting it as the recorded (not effective) level — feature-wide sync per the scribing stamp, 2 file(s) changed (trace .bee/cells/bbp-9.json)
  - [bbp-10] Placed every feature on its phase via the union of lanes and the active feature, with per-feature gates/created_at and D8-safe cell counts — feature-wide sync per the scribing stamp, 2 file(s) changed (trace .bee/cells/bbp-10.json)
  - [bbp-11] Replaced the four cell-state columns with a by-phase board (bee_phase_board_section) and re-targeted the finished-work list (bee_finished_section) onto snapshot.shipped, deduped against the phase board so a feature never renders twice; retired bee_lanes_panel. Fixed a live 404 found on the rendered beehive store: bee_feature_detail only recognized a feature through live D7 buckets/shipped, so every phase card for a lane-only, cells-archived feature 404'd. All 14 must-survive rules re-expressed against the new markup; the two 3-way tests split per area. Workspace rendering (half of the old lanes panel) is retired with no replacement in this cell, per the plan's S4 slice. — feature-wide sync per the scribing stamp, 2 file(s) changed (trace .bee/cells/bbp-11.json)
  - [bbp-12] Added validate_feature_name as the sole path-join gate for feature strings, plus presence-only promote-proposals.md reads over state/lane/cell feature names, with tests asserting no path is built for rejected names — feature-wide sync per the scribing stamp, 1 file(s) changed (trace .bee/cells/bbp-12.json)
  - [bbp-13] Added review-candidate/session join, capture-queue net-waiting count, and per-feature scribing-debt derivation to the bee reader, plus three new D6/D7-worded attention rules and their read-only board fixtures — feature-wide sync per the scribing stamp, 2 file(s) changed (trace .bee/cells/bbp-13.json)
  - [bbp-14] Rendered the backlog by status and the review queue by state (unreviewed/in-review/settled, open P1s called out) as the board's last supporting panel; a store with no candidates and no sessions reads as unknown, not zeros; fixed a real-store defect where the new PBI-title list dumped all 123 of beehive's items uncapped — feature-wide sync per the scribing stamp, 2 file(s) changed (trace .bee/cells/bbp-14.json)
  - [bbp-15] Added .bee/reservations.json reader and pure tier-mix derivation to BeeSnapshot for the process-health panel — feature-wide sync per the scribing stamp, 2 file(s) changed (trace .bee/cells/bbp-15.json)
  - [bbp-16] Fold delivery speed, sessions, worktrees, workspaces and process health into the redesigned board, retiring the running-now and worktree sections without losing any signal — feature-wide sync per the scribing stamp, 2 file(s) changed (trace .bee/cells/bbp-16.json)
  - [bbp-17] Board now collapses to one column under 700px, gives its wide phase-board columns their own overflow, adds a visible focus ring to the finished-work summary, and proves dark-scheme rules exist with no OS media-query hook that could override an explicit theme choice in either direction — verified against the real beehive store at 375px in both schemes. — feature-wide sync per the scribing stamp, 2 file(s) changed (trace .bee/cells/bbp-17.json)

(c) PATTERN CANDIDATES — candidate bee.pattern concepts, bee.polarity pitfall

None: no capped cell trace carries a deviation or a failure signature.

knowledge promote: 18 capped cell(s) mined, 1 delivery draft, 16 area bullet(s), 0 pattern candidate(s), 0 file(s) written.

---

<!-- bee:not-a-deferral: this section records a completed review with no outstanding action. -->

## Resolution — 2026-08-25, reviewed against the spec

Reviewed in a sweep of the unapplied-proposal backlog, checking each candidate
bullet against what the spec already states rather than pasting it in.

- **(a) Delivery draft** — not applied. `docs/knowledge/work/bee-board-pm/delivery.md`
  already exists as a curated record.
- **(b) Area updates** — the generated bullets are each cell's outcome in
  implementation vocabulary (function and type names), which a spec never carries
  outside its Pointers section, so none was applied as written. What the reader
  actually gained: **merged into `docs/specs/bee-cockpit.md`** — merged into "The reading order": the headline numbers are now stated as their own step between the live strip and the cards, confirmed against the running board. The lifecycle stepper and needs-attention panel this feature also shipped are NOT stated — they did not render on the live board that was checked, and asserting a shape that may since have been retired would be worse than the gap.
- **(c) Pattern candidates** — none proposed for this feature.

<!-- /bee:not-a-deferral -->
