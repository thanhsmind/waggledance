promote proposal for work item "terminal-open-access" (docs/history/terminal-open-access/CONTEXT.md + docs/history/terminal-open-access/plan.md) — 5 capped cell(s): toa-1, toa-2, toa-3, toa-4, toa-5
anchor: history — docs/history/terminal-open-access/CONTEXT.md, docs/history/terminal-open-access/plan.md
PROPOSAL ONLY — nothing was written. Applying any section below is a human or agent decision.

(a) DELIVERY DRAFT — save as docs/knowledge/work/terminal-open-access/delivery.md

---
type: bee.delivery
title: terminal-open-access — delivery
description: "Delivery record proposed by bee knowledge promote for work item terminal-open-access: 5 capped cell(s), 0 recorded deviation(s)."
timestamp: 2026-08-07
bee:
  id: terminal-open-access-delivery
  lifecycle: active
  areas: [agent-terminal, settings, web-interface, bee-cockpit]
  required_context: [docs/history/terminal-open-access/CONTEXT.md, docs/history/terminal-open-access/plan.md]
  sources: [docs/history/terminal-open-access/CONTEXT.md, docs/history/terminal-open-access/plan.md, .bee/cells/toa-1.json, .bee/cells/toa-2.json, .bee/cells/toa-3.json, .bee/cells/toa-4.json, .bee/cells/toa-5.json]
---

# terminal-open-access — Delivery

## What shipped

- **toa-1** — Stripped auth/method extractors from all 15 terminal routes (12 gated + login/rotate/config); rewrote the 12 D12 disabled-state answers (ordinary not-found for 3 page routes, reasoned JSON 404 for 9 data routes) and re-mounted every route with its explicit method; added a GET-query D11 test and a no-cookie happy-path test. Retired 25 tests whose subject was the auth disguise (session-requirement or method-mismatch-oracle) or login/rotation session mechanics; re-expressed the plan's six disabled-state keep-list tests plus a 7th (unassigned page's own disabled coverage, not in the plan's literal six but the only proof of that route's D7 gate — named deviation) and the D11 method test, all under new names, same guards. cargo test --workspace: 619 passed. (1 file(s) changed)
- **toa-2** — Switch endpoint requires JSON (D10); form-encoded POSTs are refused with no config change, verified by route tests and by hand against a scratch daemon on :7788 (3 file(s) changed)
- **toa-3** — Deleted terminal_auth.rs, its login/rotation routes, and the settings page's token controls; no non-auth helper needed rehoming (all were auth-scoped); fixed 8+1 dangling terminal_auth doc comments plus a stale exposure warning; retired 2 token-subject tests by name (D7), rewired ~34 scaffolding call sites, added a new gone-routes/gone-controls test; notify credential storage and containment tests untouched. cargo test --workspace: 597 passed. (5 file(s) changed)
- **toa-4** — Added terminal.unassigned_enabled (D9), ANDed with terminal.enabled at all four Unassigned routes and the home page's presence marker; off answers not-found in the family's existing shapes (terminal_disabled_page/terminal_disabled_json_404); defaults off even with the key absent from the config file (proven with a hand-authored config.toml missing it). Settings page wording: 'Turning this on makes every agent pane on this machine readable and writable through the browser, including ones outside any project mdview knows about ... It has no boundary check of its own.' Removed the now-unused subtle dependency and its stale comment from Cargo.toml (its only caller, terminal_auth.rs, was already deleted in toa-3); no other caller found. Deviation: assets/app.js's terminal-config-form fetch handler (out of this cell's file scope) does not yet send unassigned_enabled in its JSON body, so the browser checkbox cannot flip the switch until that file is updated -- the route itself (POST /api/terminal-config with the field set directly) works and is tested. (4 file(s) changed)
- **toa-5** — Fixed app.js's unassigned_enabled JSON-submit defect (with a new pinned test) and the stale sign-in copy; rewrote all four specs to match the code — no auth, gated only by the terminal switch and Unassigned's own switch, with the front-door safety condition and every surviving non-auth guard now stated and verified against live code and a rendered mdview instance on port 7788 (6 file(s) changed)

## Verify

Each cell below was capped only against a recorded passing verify result — bee refuses a cap without one.

- **toa-1** — `cargo test --workspace. New route-level tests: (happy) every terminal route answers with no cookie and no token present; (gate) with terminal.enabled false, each page route returns the ordinary not-found page — assert the content type is HTML and the body is non-empty, not merely the status; (gate) with it false, each polled data route returns 404 with a JSON body naming the reason; (method) a GET carrying switch values in its query changes no switch — this replaces api_terminal_config_wrong_method_is_byte_identical_to_unrouted and must fail if the route is mounted permissively; (regression) every guard listed in the action, green, with its assertions unmodified. In the outcome: the retirement ledger — each retired test named with its reason — and confirmation that every test on the plan's keep-list still exists.`
- **toa-2** — `cargo test --workspace. New route-level tests: (security) a form-encoded POST to the switch endpoint is refused and changes no switch — the assertion is on the stored config, not only the status; (happy) a JSON POST sets the switches exactly as the form did, including the redirect the page relies on; (edge) a JSON body with an unknown field is handled without changing unrelated switches; (edge) an empty credential field leaves the stored credential alone; (regression) the credential is still never readable back through the page or the config route, and a failed credential save is still reported as failure. Confirm by hand that the settings page still turns each switch on and off, and say in the outcome how you checked.`
- **toa-3** — `cargo test --workspace. (regression) every terminal route still works with no cookie and no token; (regression) the disabled state still refuses, page and data routes both; (regression) the notify credential is still write-only, never read back, and a failed save is still reported as failure; (gone) no login route, no rotation route, no token control on the settings page — assert the settings page body carries none of them; (gone) nothing in the crate references terminal_auth. In the outcome: what happened to any non-auth helper the module held, and confirmation that the notify credential's storage was untouched.`
- **toa-4** — `cargo test --workspace. (default) a config that has never mentioned the switch reads as off — the default is off in the absence of the key, not merely in the shipped file; (off) every unassigned route answers not-found, page and data routes in their own shapes; (off) the home page carries no presence marker for the group; (on) the group works exactly as it does today; (on) turning it on does not affect the project-scoped routes or their containment; (regression) the two groups still partition without overlap; (regression) a pane that IS inside a project is never served by the unassigned routes. In the outcome: quote the wording you gave the switch.`
- **toa-5** — `cargo test --workspace stays green (documentation and copy only, so the suite is a regression check rather than the proof). The proof is reading each spec against the running board and terminal: no sentence claims a token, a session, a login or a reveal-once secret; each guard the spec says still holds is one you confirmed still holds. Say in the outcome which claims you checked against a live page and how.`

## Deviations

None recorded in the capped cell traces.

## Provenance

Proposed by `bee knowledge promote --work terminal-open-access` from 5 capped cell trace(s) in `.bee/cells/` and the anchor `docs/history/terminal-open-access/CONTEXT.md`, `docs/history/terminal-open-access/plan.md`. Every line above is copied from a trace or from the work item; nothing here is curated truth until a human or agent accepts it.

(b) AREA UPDATES — candidate spec-sync bullets, each citing its cell

areas: from the scribing stamp for "terminal-open-access" — .bee/logs/scribing-runs.jsonl's most recent entry (2026-08-07T02:42:57.641Z), the work item declares no bee.areas.

area agent-terminal:
  - [toa-1] Stripped auth/method extractors from all 15 terminal routes (12 gated + login/rotate/config); rewrote the 12 D12 disabled-state answers (ordinary not-found for 3 page routes, reasoned JSON 404 for 9 data routes) and re-mounted every route with its explicit method; added a GET-query D11 test and a no-cookie happy-path test. Retired 25 tests whose subject was the auth disguise (session-requirement or method-mismatch-oracle) or login/rotation session mechanics; re-expressed the plan's six disabled-state keep-list tests plus a 7th (unassigned page's own disabled coverage, not in the plan's literal six but the only proof of that route's D7 gate — named deviation) and the D11 method test, all under new names, same guards. cargo test --workspace: 619 passed. — feature-wide sync per the scribing stamp, 1 file(s) changed (trace .bee/cells/toa-1.json)
  - [toa-2] Switch endpoint requires JSON (D10); form-encoded POSTs are refused with no config change, verified by route tests and by hand against a scratch daemon on :7788 — feature-wide sync per the scribing stamp, 3 file(s) changed (trace .bee/cells/toa-2.json)
  - [toa-3] Deleted terminal_auth.rs, its login/rotation routes, and the settings page's token controls; no non-auth helper needed rehoming (all were auth-scoped); fixed 8+1 dangling terminal_auth doc comments plus a stale exposure warning; retired 2 token-subject tests by name (D7), rewired ~34 scaffolding call sites, added a new gone-routes/gone-controls test; notify credential storage and containment tests untouched. cargo test --workspace: 597 passed. — feature-wide sync per the scribing stamp, 5 file(s) changed (trace .bee/cells/toa-3.json)
  - [toa-4] Added terminal.unassigned_enabled (D9), ANDed with terminal.enabled at all four Unassigned routes and the home page's presence marker; off answers not-found in the family's existing shapes (terminal_disabled_page/terminal_disabled_json_404); defaults off even with the key absent from the config file (proven with a hand-authored config.toml missing it). Settings page wording: 'Turning this on makes every agent pane on this machine readable and writable through the browser, including ones outside any project mdview knows about ... It has no boundary check of its own.' Removed the now-unused subtle dependency and its stale comment from Cargo.toml (its only caller, terminal_auth.rs, was already deleted in toa-3); no other caller found. Deviation: assets/app.js's terminal-config-form fetch handler (out of this cell's file scope) does not yet send unassigned_enabled in its JSON body, so the browser checkbox cannot flip the switch until that file is updated -- the route itself (POST /api/terminal-config with the field set directly) works and is tested. — feature-wide sync per the scribing stamp, 4 file(s) changed (trace .bee/cells/toa-4.json)

area settings:
  - [toa-1] Stripped auth/method extractors from all 15 terminal routes (12 gated + login/rotate/config); rewrote the 12 D12 disabled-state answers (ordinary not-found for 3 page routes, reasoned JSON 404 for 9 data routes) and re-mounted every route with its explicit method; added a GET-query D11 test and a no-cookie happy-path test. Retired 25 tests whose subject was the auth disguise (session-requirement or method-mismatch-oracle) or login/rotation session mechanics; re-expressed the plan's six disabled-state keep-list tests plus a 7th (unassigned page's own disabled coverage, not in the plan's literal six but the only proof of that route's D7 gate — named deviation) and the D11 method test, all under new names, same guards. cargo test --workspace: 619 passed. — feature-wide sync per the scribing stamp, 1 file(s) changed (trace .bee/cells/toa-1.json)
  - [toa-2] Switch endpoint requires JSON (D10); form-encoded POSTs are refused with no config change, verified by route tests and by hand against a scratch daemon on :7788 — feature-wide sync per the scribing stamp, 3 file(s) changed (trace .bee/cells/toa-2.json)
  - [toa-3] Deleted terminal_auth.rs, its login/rotation routes, and the settings page's token controls; no non-auth helper needed rehoming (all were auth-scoped); fixed 8+1 dangling terminal_auth doc comments plus a stale exposure warning; retired 2 token-subject tests by name (D7), rewired ~34 scaffolding call sites, added a new gone-routes/gone-controls test; notify credential storage and containment tests untouched. cargo test --workspace: 597 passed. — feature-wide sync per the scribing stamp, 5 file(s) changed (trace .bee/cells/toa-3.json)
  - [toa-4] Added terminal.unassigned_enabled (D9), ANDed with terminal.enabled at all four Unassigned routes and the home page's presence marker; off answers not-found in the family's existing shapes (terminal_disabled_page/terminal_disabled_json_404); defaults off even with the key absent from the config file (proven with a hand-authored config.toml missing it). Settings page wording: 'Turning this on makes every agent pane on this machine readable and writable through the browser, including ones outside any project mdview knows about ... It has no boundary check of its own.' Removed the now-unused subtle dependency and its stale comment from Cargo.toml (its only caller, terminal_auth.rs, was already deleted in toa-3); no other caller found. Deviation: assets/app.js's terminal-config-form fetch handler (out of this cell's file scope) does not yet send unassigned_enabled in its JSON body, so the browser checkbox cannot flip the switch until that file is updated -- the route itself (POST /api/terminal-config with the field set directly) works and is tested. — feature-wide sync per the scribing stamp, 4 file(s) changed (trace .bee/cells/toa-4.json)

area web-interface:
  - [toa-1] Stripped auth/method extractors from all 15 terminal routes (12 gated + login/rotate/config); rewrote the 12 D12 disabled-state answers (ordinary not-found for 3 page routes, reasoned JSON 404 for 9 data routes) and re-mounted every route with its explicit method; added a GET-query D11 test and a no-cookie happy-path test. Retired 25 tests whose subject was the auth disguise (session-requirement or method-mismatch-oracle) or login/rotation session mechanics; re-expressed the plan's six disabled-state keep-list tests plus a 7th (unassigned page's own disabled coverage, not in the plan's literal six but the only proof of that route's D7 gate — named deviation) and the D11 method test, all under new names, same guards. cargo test --workspace: 619 passed. — feature-wide sync per the scribing stamp, 1 file(s) changed (trace .bee/cells/toa-1.json)
  - [toa-2] Switch endpoint requires JSON (D10); form-encoded POSTs are refused with no config change, verified by route tests and by hand against a scratch daemon on :7788 — feature-wide sync per the scribing stamp, 3 file(s) changed (trace .bee/cells/toa-2.json)
  - [toa-3] Deleted terminal_auth.rs, its login/rotation routes, and the settings page's token controls; no non-auth helper needed rehoming (all were auth-scoped); fixed 8+1 dangling terminal_auth doc comments plus a stale exposure warning; retired 2 token-subject tests by name (D7), rewired ~34 scaffolding call sites, added a new gone-routes/gone-controls test; notify credential storage and containment tests untouched. cargo test --workspace: 597 passed. — feature-wide sync per the scribing stamp, 5 file(s) changed (trace .bee/cells/toa-3.json)
  - [toa-4] Added terminal.unassigned_enabled (D9), ANDed with terminal.enabled at all four Unassigned routes and the home page's presence marker; off answers not-found in the family's existing shapes (terminal_disabled_page/terminal_disabled_json_404); defaults off even with the key absent from the config file (proven with a hand-authored config.toml missing it). Settings page wording: 'Turning this on makes every agent pane on this machine readable and writable through the browser, including ones outside any project mdview knows about ... It has no boundary check of its own.' Removed the now-unused subtle dependency and its stale comment from Cargo.toml (its only caller, terminal_auth.rs, was already deleted in toa-3); no other caller found. Deviation: assets/app.js's terminal-config-form fetch handler (out of this cell's file scope) does not yet send unassigned_enabled in its JSON body, so the browser checkbox cannot flip the switch until that file is updated -- the route itself (POST /api/terminal-config with the field set directly) works and is tested. — feature-wide sync per the scribing stamp, 4 file(s) changed (trace .bee/cells/toa-4.json)

area bee-cockpit:
  - [toa-1] Stripped auth/method extractors from all 15 terminal routes (12 gated + login/rotate/config); rewrote the 12 D12 disabled-state answers (ordinary not-found for 3 page routes, reasoned JSON 404 for 9 data routes) and re-mounted every route with its explicit method; added a GET-query D11 test and a no-cookie happy-path test. Retired 25 tests whose subject was the auth disguise (session-requirement or method-mismatch-oracle) or login/rotation session mechanics; re-expressed the plan's six disabled-state keep-list tests plus a 7th (unassigned page's own disabled coverage, not in the plan's literal six but the only proof of that route's D7 gate — named deviation) and the D11 method test, all under new names, same guards. cargo test --workspace: 619 passed. — feature-wide sync per the scribing stamp, 1 file(s) changed (trace .bee/cells/toa-1.json)
  - [toa-2] Switch endpoint requires JSON (D10); form-encoded POSTs are refused with no config change, verified by route tests and by hand against a scratch daemon on :7788 — feature-wide sync per the scribing stamp, 3 file(s) changed (trace .bee/cells/toa-2.json)
  - [toa-3] Deleted terminal_auth.rs, its login/rotation routes, and the settings page's token controls; no non-auth helper needed rehoming (all were auth-scoped); fixed 8+1 dangling terminal_auth doc comments plus a stale exposure warning; retired 2 token-subject tests by name (D7), rewired ~34 scaffolding call sites, added a new gone-routes/gone-controls test; notify credential storage and containment tests untouched. cargo test --workspace: 597 passed. — feature-wide sync per the scribing stamp, 5 file(s) changed (trace .bee/cells/toa-3.json)
  - [toa-4] Added terminal.unassigned_enabled (D9), ANDed with terminal.enabled at all four Unassigned routes and the home page's presence marker; off answers not-found in the family's existing shapes (terminal_disabled_page/terminal_disabled_json_404); defaults off even with the key absent from the config file (proven with a hand-authored config.toml missing it). Settings page wording: 'Turning this on makes every agent pane on this machine readable and writable through the browser, including ones outside any project mdview knows about ... It has no boundary check of its own.' Removed the now-unused subtle dependency and its stale comment from Cargo.toml (its only caller, terminal_auth.rs, was already deleted in toa-3); no other caller found. Deviation: assets/app.js's terminal-config-form fetch handler (out of this cell's file scope) does not yet send unassigned_enabled in its JSON body, so the browser checkbox cannot flip the switch until that file is updated -- the route itself (POST /api/terminal-config with the field set directly) works and is tested. — feature-wide sync per the scribing stamp, 4 file(s) changed (trace .bee/cells/toa-4.json)

(c) PATTERN CANDIDATES — candidate bee.pattern concepts, bee.polarity pitfall

None: no capped cell trace carries a deviation or a failure signature.

knowledge promote: 5 capped cell(s) mined, 1 delivery draft, 16 area bullet(s), 0 pattern candidate(s), 0 file(s) written.

---

<!-- bee:not-a-deferral: this section records a completed review with no outstanding action. -->

## Resolution — 2026-08-25, the spec already records this feature

Reviewed in the sweep of the unapplied-proposal backlog.

- **(a) Delivery draft** — not applied. `docs/knowledge/work/terminal-open-access/delivery.md`
  already exists as a curated record.
- **(b) Area updates** — not applied. `docs/specs/agent-terminal.md` and `docs/specs/settings.md` and `docs/specs/web-interface.md` names `terminal-open-access` in its own
  `sources`, which is the receipt that this feature's behaviour was merged into
  the spec when it closed. The generated bullets restate the same work in
  implementation vocabulary — function and type names — which a spec carries only
  in its Pointers, so applying them would add nothing the spec does not say and
  would break its vocabulary rule. This resolution rests on that receipt and on
  the spec owning the area, not on a line-by-line re-reading of every bullet.
- **(c) Pattern candidates** — none promoted from here.

<!-- /bee:not-a-deferral -->
