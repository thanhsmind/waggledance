# Product Backlog

<!--
GENERATED FILE — do not hand-edit.
Rendered by `bee backlog render` from event-sourced PBI records in .bee/backlog.jsonl (backlog-unification D1/D3).
Regenerate: `bee backlog render --write`. Check freshness: `bee backlog render --check`.
Deterministic: byte-identical for the same backlog.jsonl contents — status-grouped, id-sorted entries, LF endings,
never a generation timestamp or any other wall-clock value.
-->

| ID | Story | CoS | Status | Feature |
|----|-------|-----|--------|---------|
| p-498dd298 | doctor reorders every key in the user's ~/.claude.json | serde_json is declared without preserve_order, so its Map is a BTreeMap and each doctor write alphabetically reorders the whole file. Content survives; ordering and formatting do not. Fix by enabling preserve_order, with a test asserting an unrelated key block keeps its original order across a fix. | proposed | — |
| p-9212cae8 | herding_agent_argv_from_config accepts the object form of a herding.agents entry | An entry written as {"argv": [...], "workspace_trust": {...}} — the shape bee itself writes for agy-flash in beehive — resolves to its argv instead of None, so selecting it no longer makes the board refuse with "records no herding.agent_command". The array form keeps working unchanged. | proposed | — |
| p-a541ffc2 | Optional swarm manifest declaring which registered projects form one coordinated group | A named group of registered projects, each carrying a role, an architectural level and its depends_on edges, survives across sessions and groups the cross-project board into rows. Read-only: membership never implies dispatch permission — orchestration.enabled stays the only gate. The manifest is data only; the conductor over it is a Lieutenant that lives OUTSIDE the daemon, as a bee skill driving the shipped MCP tools (ask_state, dispatch, await, runs), so no new tool and no daemon change is required — satisfying orchestrator-dispatch D1 rather than superseding it. Second source: the SwarmForge distillation (docs/history/research/swarmforge-platoon-layer.md), whose pack layer (role-worktrees, tmux, handoffd, pack cockpit) is explicitly NOT adopted. | proposed | — |
| p-cd0eadfc | bee backlog add refuses every argument shape | bee backlog add is unusable in this build: even the minimal form (--title alone) is refused with unsupported argument shape, while bee backlog pbi add works. Either the dispatcher shape or the help text is wrong. RE-CHECKED 2026-08-25: the four-flag form works — 'backlog add --type debt --title X --severity P3 --layer board' appended cleanly. --type, --severity and --layer are all required, so --title alone refusing is the schema working, not a defect; --type also takes a closed set (debt/friction/finding/pbi), which 'feature' and 'idea' are not in. What remains true is the message: the refusal says 'unsupported argument shape' without naming the missing flag or the legal --type values. Narrow this item to that message, or decline it. | proposed | — |
| p-e5b770fe | Parse install.ps1 in CI — no pwsh on the dev machine | A windows-latest job at minimum runs [ScriptBlock]::Create over install.ps1's contents. The script was rewritten for the rename (repo, binary name, and the move to %LOCALAPPDATA%\Programs\waggledance) but has never been syntax-checked: pwsh is absent locally, so it is review-verified only. | proposed | — |
| p-e62c967c | Prove the desktop crate builds — it never has | cargo build --manifest-path crates/waggledance-desktop/Cargo.toml completes in CI or on a machine with pkg-config, libwebkit2gtk and gtk3. Today it fails inside libdbus-sys before reaching the crate's own code, and the same failure occurs against the old manifest path, so it is not a rename defect — but the crate is unproven. It is excluded from the workspace, so cargo test --workspace can never catch it. | proposed | — |
| p-e9386ebb | As the human, I want an inbox showing the letters bee leaves after each unattended run, so I can read what happened overnight without replaying a session. | bee writes one markdown letter per unattended RUN at <project>/.bee/human-mailbox/<UTC-ts>-<run-slug>.md; this item is the waggledance half, the screen that shows them. Full spec: beehive docs/discovery/human-mailbox/MAP.md (decisions D1-D14, D16).  RECORD. YAML frontmatter is the contract, the body is human prose. Frontmatter: subject (REQUIRED, one plain sentence, no bee vocabulary - this is the inbox row), run, project, filed_at, a read/unread status field, items[] {what, files[], commit, proof, departure: null \| {what, why, kind}}, needs_you[] {id, what it blocks}. Body sections, empty ones dropped: Done / Where I departed from the plan and why / Broken or unfinished / Needs your call / Next. A letter written when a feature closes carries the same shape plus architecture, behaviour and usage.  DONE WHEN. The inbox lists letters newest-first by subject with unread distinguished; opening one renders the body; marking read flips bee's status field THROUGH A BEE COMMAND, never a direct write into .bee/ - D6 keeps bee the single writer of its own store, so waggledance-core/src/bee.rs stays a pure reader. A run that died without finishing reads plainly as unfinished. needs_you items are shown but not answerable in this round; their ids exist so a reply surface stays reachable without changing the record.  REUSE ALREADY HERE. bee.rs already reads .bee/ across projects; render.rs already parses frontmatter; notify_store.rs plus notify/telegram.rs could push the subject line, which is out of scope for this item. | proposed | — |

## Done / Declined

- [p-097cf752] Agent-facing MCP query surface: waggledance answers questions instead of agents re-reading files — done
- [p-42cbde3a] waggledance_dispatch resolves a preset against the target project herding.agents — done
- [p-732028ad] docs/backlog.md line 3 still names the old command — done
- [p-a953e7c0] Spawn destination resolves off a workspace anchor, so a project with panes can still be undispatchable — done
- [p-8d9e51f4] As an orchestrator, I want to coordinate several registered projects as one swarm, so that a multi-project effort has a single conductor instead of one agent per repo — declined
